# 2026-07-26 — day reconstruction (written 19:47 PDT)

Reconstructed from `git log` and `br`, not from live observation: the nine
orchestrator windows that ran 10:15–15:02 left no briefing behind, because the
briefing writer overwrote `docs/briefings/2026-07-26.md` instead of appending.
That day-file therefore records only session `4dd46fe1`'s final 7 minutes. The
overwrite was fixed globally in the skill later the same day (hour-stamped
filenames, `%Y-%m-%d-%H.md`); this file is the first under that convention.

## Totals

- 68 commits between 00:00 and 19:32; 95 files changed, +5768 / −319.
- Orchestrator windows observed handing off: 10:15, 10:45, 11:15, 11:41, 12:11,
  12:50, 13:27, 14:08, 14:34, 15:02. The 15:02 session ran 4 idle iterations
  (all `br ready` issues carried `needs-human`) and paused at 5h=88%.
- Released v0.4.0 at 19:44 (commit `8283b65`, tag `v0.4.0`, run 30232883614).

## Work landed

**Pre-dial parameter validation** — the day's dominant theme, ~15 commits.
Validation moved ahead of the Godot dial across the CLI, gwpop, mcpserver, and
scenario loader: `--port` range (`917ddd9`), empty required strings / negative
ints / bad enums (`8156fe6`), wait+input subcommands before the port check
(`67f9777`), `assert_performance` monitor name (`a5d156a`), `get_performance`
monitor allowlist (`50ea6bf`), declared Min/Max on every numeric MCP tool param
(`c94d36a`), `boundedInt` rejecting fractional args (`d244780`), scenario
`target.port` (`e949e52`) and negative `target.timeout_ms` (`71724f8`),
`screenshot_diff` threshold + pixel sensitivity at load time (`c6f7438`) and at
the CLI (`c406bd5`), dotted Windows device names in baseline names (`4586b5d`),
`godot_connect` / `godot_launch` port range (`194208e`), and params validated
before dialing at all (`521c832`).

**`setup` command hardening** — `--help` exits 0 (`a3fa409`), warns when the
installed addon is stale against the embedded version (`68ec3c7`), validates
`project.godot` before copying (`4f46a5b`), stages the copy and atomically swaps
it into place (`4372123`), accepts `--force` after the project path (`dd5afcf`).

**Scenario runner and reporting** — deduped `Actions()` so screenshot lists once
(`e7d4f52`), added a `phase` field to rpc-trace so teardown RPCs stop being
attributed to step 0 (`c058b1c`), stopped doubling "step" in failure summaries
(`dc5f418`), excluded teardown from step pass/fail counts (`f730663`),
normalized pre-step failure reports (`a05056a`), resolved the default diff dir
scenario-relative like baselines (`6930d36`, documented in `1af5238`).

**Runtime and connection** — `StagehandServer` keeps servicing its socket loop
while the tree is paused (`0a096e4`), longer first window on the
quit-on-disconnect grace (`db0977f`), parked calls wake promptly when reconnect
gives up (`38469c8`), Windows GUI Godot launches from WSL (`684bbc1`),
`--stagehand-port=` honored before the `--` separator (`873cdb0`),
`wait_for_property` compares stringified `expected_value` target-aware
(`4a26d7d`), `Capture` routed through `visual.Params` so selector crops actually
crop (`6fff361`), unsupported/zero-sized crop errors instead of silently
falling back to full-page (`7d7cb29`), dial timeout classified `ExitConnection`
rather than `ExitTimeout` (`6030cbb`).

**CI** — scenario-smoke wired to the full godot-tagged suite, which had been
running 2 of 28 real-Godot tests (`602d301`); shellcheck gate added for
`scripts/*.sh` (`b89ee3d`).

**Docs and growth** — hostcompat design doc corrected, approved, and given
Amendment 7 to scale the suite to N hosts (`1680bd2`, `566e24a`, `3e29523`);
`examples/minimal-game` turned into a one-command watch-an-agent-play onramp
(`e3510f3`) with its minimum Godot version corrected to 4.3 (`7c8c082`);
directory submission kit for MCP/Godot listings (`ee9a68f`); source-level
competitor analysis of godot-mcp-enhanced (`e4da1cb`) and the resulting removal
of the blanket runtime-vs-editor exclusivity claim (`cba4d8d`); adoption metrics
rewritten around user effort rather than stars (`9b74779`); release asset matrix
and download URLs verified against v0.3.1 (`68ca35a`).

## v0.4.0 release

Protocol unchanged (`gwp/1`), so the bump is release-version only. Gates run
before the commit, all clean: `go build` / `go vet` / `go test ./...`;
`shellcheck scripts/*.sh`; `ci-gdscript-warnings.sh check` (test_project and
minimal-game both load clean with every `gdscript/warnings/*` at error);
`go test -tags=godot ./...` — 14 packages green on Godot 4.6.2
(`internal/launch` 110.9s, `internal/godotconn` 16.4s); `go test
./internal/version/` after the bump; `./build-release.sh 0.4.0 --verify-only`.

Published assets, all smoke-tested by the workflow (downloaded, executed, tag
version confirmed): `linux-amd64` 11.72 MB, `darwin-amd64` 12.01 MB,
`darwin-arm64` 11.33 MB, `windows-amd64.exe` 11.91 MB.

Behavior changes callers may notice: dial timeout now exits `ExitConnection`,
not `ExitTimeout`; screenshot errors on an unsupported or zero-sized crop
instead of silently capturing full-page.
